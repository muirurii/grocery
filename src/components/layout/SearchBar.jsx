const SearchBar = () => {
    return (
        <div className="search-bar">
            <input type="text" placeholder="search products" />
            <button> &copy; </button>
        </div>
    )
}

export default SearchBar
